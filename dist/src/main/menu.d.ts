import { Menu, BrowserWindow } from 'electron';
/**
 * Creates and returns the application menu with keyboard shortcuts
 */
export declare function createApplicationMenu(mainWindow: BrowserWindow): Menu;
/**
 * Sets up the application menu
 */
export declare function setupApplicationMenu(mainWindow: BrowserWindow): void;
